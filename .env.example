# Copier en .env (jamais commité) avant `docker compose up -d`.
# Voir database/postgresql/003_provisioning.md pour le provisioning complet
# du rôle applicatif tracksys_app (distinct de ce mot de passe superutilisateur).

POSTGRES_SUPERUSER_PASSWORD=change_me_dev_only
